<#if Chg5MinStkTest.upCount gt 0><p><font color="blue">${Chg5MinStkTest.currHH}</font>:<font color="blue">${Chg5MinStkTest.currmm}</font>:<font color="blue">${Chg5MinStkTest.currss}</font> 以下股票过去5分钟出现快速拉升：<#list Chg5MinStkTest.revUpList as key><font color="blue">${key['stkName']}</font>快速拉升<font color="blue">${key['pct']}</font>%，报 <font color="blue">${key['price']}</font>元<#if key_has_next>；<#else>。</#if></#list></p></#if><#if Chg5MinStkTest.downCount gt 0><p><font color="blue">${Chg5MinStkTest.currHH}</font>:<font color="blue">${Chg5MinStkTest.currmm}</font>:<font color="blue">${Chg5MinStkTest.currss}</font>以下股票过去5分钟出现快速回落：</p><#list Chg5MinStkTest.revDownList as key><font color="blue">${key['stkName']}</font>快速回落<font color="blue">${key['pct']}</font>%，报 <font color="blue">${key['price']}</font>元<#if key_has_next>；<#else>。</#if></#list></p></#if>